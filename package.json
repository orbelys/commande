{
  "name": "commande",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Interface mobile de pilotage a distance de la Suite PSE (Electron)",
  "scripts": {
    "dev": "vite",
    "dev:lan": "vite --host",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "typecheck": "tsc -b --noEmit",
    "deploy": "VITE_BASE=/commande/ vite build && rm -rf ../PSE/commande && mkdir -p ../PSE/commande && cp -R dist/. ../PSE/commande/ && echo 'Copié dans PSE/commande — publiez avec GitHub Desktop (dépôt PSE)'"
  },
  "dependencies": {
    "firebase": "^12.18.0",
    "react": "^19.1.1",
    "react-dom": "^19.1.1",
    "react-router-dom": "^7.8.2"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "@types/react": "^19.1.12",
    "@types/react-dom": "^19.1.9",
    "@vitejs/plugin-react": "^5.0.2",
    "typescript": "^5.9.2",
    "vite": "^7.1.4"
  },
  "allowScripts": {
    "esbuild@0.28.2": true
  }
}
