# Copier ce fichier en ".env.local" quand Firebase sera branche.
# .env.local est ignore par Git (voir .gitignore).
# Rien de tout cela n'est necessaire aujourd'hui : l'app tourne en mode demo.

# Transport actif : "mock" (simulation locale) ou "firebase" (plus tard)
VITE_TRANSPORT=mock

# --- Firebase (a remplir uniquement le moment venu) ---
# VITE_FIREBASE_API_KEY=
# VITE_FIREBASE_AUTH_DOMAIN=
# VITE_FIREBASE_PROJECT_ID=
# VITE_FIREBASE_APP_ID=
